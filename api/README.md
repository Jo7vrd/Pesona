# API — Jelajah Kei Kecil

Backend Go + Gin + PostgreSQL + GORM. Dibangun pada Fase 8 sesuai rencana pengembangan.

Arsitektur: Clean Architecture (`handler → service → repository`), JWT auth, RBAC, penyimpanan foto di Cloudflare R2. Lihat `docs/` di root repo untuk detail.
